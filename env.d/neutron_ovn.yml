component_skel:
  neutron_ovn_controller:
    belongs_to:
      - neutron_all
    properties:
      is_metal: true
  neutron_ovn_northd:
    belongs_to:
      - neutron_all
    properties:
      is_metal: true

container_skel:
  neutron_agents_container:
    contains: {}
  neutron_ovn_northd_container:
    belongs_to:
      - network_containers
    contains:
      - neutron_ovn_northd
    properties:
      is_metal: true
